# Section 7 — Data Flow diagram

Mermaid sequence diagram — happy path, all component interactions, using real component names. Must compile without errors.

```mermaid
sequenceDiagram
    [real component names]
```
